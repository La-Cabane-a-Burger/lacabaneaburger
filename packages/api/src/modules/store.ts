export const Store = `
    type Store {
        id: ID!
        city: String!
        phone: String
        postal_code: Int
        address: String
        latitude: Int
        longitude: Int
    }
`

export const StoreResolver = {};
